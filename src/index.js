import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root")
);

// var numbers = [3, 56, 2, 48, 5];

// const double = numbers.map(num => num * 2)
// console.log(double);

// const greaterTen = numbers.filter(num => num > 10);
// console.log(greaterTen);

// const total = numbers.reduce((accum, num) => accum + num);
// console.log(total);

// const findForty = numbers.find(num => num >= 40);
// console.log(findForty);

// const findIndexFifty = numbers.findIndex(num => num >= 50);
// console.log(findIndexFifty);